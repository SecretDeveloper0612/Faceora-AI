# Clear Expo cache script
#!/usr/bin/env bash
# Stop any running Expo process (if running in this terminal, you may need to press Ctrl+C manually)
# Then start Expo with a cleared cache
npx expo start -c
